# ---- Core (required) ----
streamlit>=1.40
python-dotenv>=1.0
pandas>=2.2
openai>=1.59        # OpenAI-compatible client for GitHub Models (and Azure OpenAI)
mcp>=1.0            # Model Context Protocol client (Microsoft Learn MCP server)

# ---- Microsoft Foundry (LLM_PROVIDER=azure) ----
azure-ai-projects>=2.2
azure-identity>=1.19

# ---- Monitoring: OpenTelemetry -> Azure Application Insights ----
azure-monitor-opentelemetry>=1.6
opentelemetry-instrumentation-fastapi>=0.48b0

# ---- Hosted agent (Foundry Agent Service deployment) ----
fastapi>=0.115
uvicorn>=0.30

# ---- Dev / test ----
pytest>=8.3
